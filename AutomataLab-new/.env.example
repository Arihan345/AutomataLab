# Copy to .env (or .env.local) and set for local overrides. Not needed for
# local dev by default — VITE_API_URL falls back to http://localhost:4000.
# In production, set this in your hosting platform's environment variable
# settings to the deployed backend's URL.
VITE_API_URL=
